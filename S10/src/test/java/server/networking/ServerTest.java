package server.networking;

class ServerTest {

    // Mocking a blocking IO like networking is outside the scope of this course.
    // Server instance reponsibility is only to trigger calls to other object, we will only tests those other objects logic

}